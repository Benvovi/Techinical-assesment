apiVersion: v1
kind: ConfigMap
metadata:
  name: titanic-api-config
  namespace: titanic-api
data:
  FLASK_ENV: "production"
  DATABASE_HOST: "titanic-db-service"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "titanic_db"
  LOG_LEVEL: "INFO"
  METRICS_ENABLED: "true"
  TRACING_ENABLED: "true"
